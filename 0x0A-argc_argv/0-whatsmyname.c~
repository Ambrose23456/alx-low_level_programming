#include "main.h"

/**
 *main - entry point
 *@argc: argument count
 *@argv: array of argument count
 *Return: always(0)
 */
int main(int argc, char *argv[])
{
	printf ("%s", argv[argc]);
	Return (0);
}
