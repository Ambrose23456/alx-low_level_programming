#include <stdio.h>
#include "main.h"

/**
 *main - entry point
 *@argc: argument count
 *@argv: array of argument count
 *Return: always(0)
 */
int main(int argc, char *argv[])
{
	argc = 0;
	printf ("%s\n", argv[argc]);
	return (0);
}
