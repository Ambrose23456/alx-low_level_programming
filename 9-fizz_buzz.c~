#include <stdio.h>

/**
 *main - entry point
 *
 *Return: Always(0)
 */
int main(void)
{
	int i;

	for (i = 1; i < 101; i++)
	{
		if (i % 3 == 0)
			printf("fizz ");
		else if (i % 5 == 0)
			printf("buzz ");
		else if (i % 15 == 0)
			printf("fizzbuzz ");
		else
		printf("%d ", i);
	}
	return (0);
}
