#include "main.h"
/**
 *print_last_digit - prints last digit of a number
 *@n: is the parameter to be received by the function
 *
 *Return: last digit
 */
int print_last_digit(int n)
{
	n = n % 10;
	return (n);
}
